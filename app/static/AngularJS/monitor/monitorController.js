registrationModule.controller('monitorController', function($scope, $rootScope, alertFactory, monitorRepository) {
	openCloseNav();
    $scope.init = function() {
        console.log('Logre entrar al Monitor :D')

    };
    
});